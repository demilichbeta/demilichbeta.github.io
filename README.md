# 物流夜班點貨 PWA 0.052

## 0.052 主要變更
- 二次分理 `＋1` 後，待轉數會先加入現場「應有」；不影響03:00／05:00快速回報。
- 按「轉完成」時，待轉搬到二分完成，現場應有不會再重複增加；同時把現場實盤 `actual` 依籠／板同步＋相同數量，代表6點後這批已盤點完成。
- 過境貨與回倉目前時段加減新增低亮度觸及回饋，保留震動但不使用夜班＋1的高亮效果。
- 其他功能排序調整：中班數量 → 回倉紀錄 → 過境貨 → 提前載走 → 二次分理 → 特殊盤點 → 事件紀錄 → 班次／備份。
- 特殊盤點卡片、頁籤、輸入與預覽色塊收斂到既有深色 UI／語意色系，資料與確認邏輯不變。

## 二次分理資料規則
- `online`：待處理。會加入現場應有，但不進快速回報。
- `secondary`：已轉完成。轉完成時由 `online` 搬入，因此應有總數不重複增加。
- `actual`：現場實盤。轉完成時同步＋同站同載具的完成數。
- 單站與全部轉完成均使用同一套規則。

## 特殊盤點
- 特殊盤點仍是高權限入口，不建立第二份中班／夜班／過境／現場數量。
- 輸入先留在本頁草稿；按「確認此站」才以差額事件寫回正式 `events`。
- 一般提前載走、00前已載走、二次分理不會被特殊盤點重設。
- 二分待轉與已完成都會反映在特殊盤點的現場應有預覽。

## 資料結構
- Schema：16（0.052不升Schema）。
- `events` 仍是正式數量唯一來源。
- `specialInventory` 只保存確認時間與資料簽章。
- 0.051資料可直接使用，不需要額外遷移。

## 保留既有功能
- 0.051：特殊盤點共用正式資料與確認簽章。
- 0.050：現場盤點中／夜／過／總 2×2來源資訊。
- 0.049：二次分理待處理固定顯示＋滑道臨時選站。
- 0.047：00前已載走獨立資料區；最終籠／板總量仍加回00前已載走。
- 0.046：05:00複製排除 `NS6`、`TS1`、`TS3`；滑道調整與 `Nt24`。
- 0.033：快速回報全形固定欄寬格式。
- 0.048的現場應有負數截斷仍不採用。

## 版本資產
- `styles-0.052.css`
- `logic-0.052.js`
- `app-0.052.js`
- `manifest-0.052.json`
- `service-worker-0.052.js`
