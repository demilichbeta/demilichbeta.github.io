# 物流夜班點貨 PWA 0.059

## 0.059 主要變更

- 回倉紀錄既有站所清單最下方新增 `DC6`、`TS5`、`DC7`。
- 回倉複製回報文字的固定來源順序同步在原清單後追加 `DC6 → TS5 → DC7`。
- 其他頁面的站所清單、預設載具、快速回報與統計邏輯不變。
- Schema：16（0.059 不升 Schema）。

## 資料與升級

- 直接沿用 0.056 的 IndexedDB 資料。
- 新增的三個回倉來源只擴充固定來源清單，不建立新資料欄位。

## 版本資產

- `styles-0.059.css`
- `logic-0.059.js`
- `app-0.059.js`
- `manifest-0.059.json`
- `service-worker-0.059.js`
