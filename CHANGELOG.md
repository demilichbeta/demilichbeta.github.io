# 0.056

- 修正 0.055 新增樣式被錯誤寫成字面 `\\n`，導致瀏覽器無法解析整段新增 CSS 的問題。
- 恢復夜班＋1浮動回饋，並讓過境貨／回倉＋1的低亮度浮動回饋正常顯示。
- 恢復過境貨「目前載具 = 站所預設載具」時的左側 3px 粉紅提示線。
- 保留 0.055 的 YT → HT → ET 排序與 05:00 複製順序。
- Schema 維持 16；不修改任何盤點／統計資料邏輯。

## ＋1觸及回饋
- 夜班＋1改為向上為主的隨機浮動位置；連續點擊不固定在同一位置，且浮動節點可同時存在。
- 過境貨與回倉紀錄的＋1加入同型但較低亮度的浮動回饋。
- 原有過境／回倉低干擾卡片觸及回饋保留。

## 過境貨預設載具提示
- 過境貨展開站所卡時，若目前選擇載具正是該站夜班預設載具，卡片左側顯示3px低調MOMO粉紅線。
- 切到非預設載具時粉紅線消失，不改數量與操作邏輯。

## YT／HT／ET排序
- 所有同時呈現YT、HT、ET的系列與回報順序統一為 YT → HT → ET。
- 05:00快速盤點範圍與複製文字同步採用新順序。
- 00前已載走常用站所中的三站順序同步調整。

## 資料與計算
- Schema維持16。
- 不修改任何事件結構、數量公式或特殊盤點資料。
