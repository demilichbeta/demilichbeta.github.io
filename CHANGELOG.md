# 0.052 更新紀錄

## 二次分理：6點後「待轉先算應有、轉完成同步實盤」
- 二次分理 `＋1` 後，待處理 `online` 立即加入現場「應有」計算，但仍不列入03:00／05:00快速回報。
- 「轉完成」時，待處理數搬到 `secondary`，因此現場應有總數不會再次增加。
- 同一個轉完成 operation 會再將該站、該載具的 `actual` 同步增加相同數量，代表6點後此批二分已完成現場確認。
- 單站轉完成與「全部轉完成」都套用相同規則；籠／板依各自待處理數分開同步。
- 轉完成同時更新該載具的現場已盤確認時間；不修改中班／夜班／過境來源數。
- 例：原應有5、實盤5；二分＋1後為應有6／實盤5；轉完成後為應有6／實盤6。

## 過境貨／回倉紀錄：低亮度觸及回饋
- 過境貨站所加減後，站所卡會短暫壓暗／描邊回饋，強度低於夜班＋1。
- 回倉目前時段來源加減後，同樣提供低干擾卡片回饋。
- 保留既有震動回饋；新增視覺回饋只用來確認「有按到」，不使用夜班的高亮動畫。

## 其他功能排序
- 「回倉紀錄」與「特殊盤點」交換位置。
- 新順序：中班數量 → 回倉紀錄 → 過境貨 → 提前載走 → 二次分理 → 特殊盤點 → 事件紀錄 → 班次／備份。

## 特殊盤點 UI 微調
- 不改特殊盤點資料結構與高權限確認邏輯。
- 卡片圓角、背景、邊框、載具頁籤、輸入框與預覽格改回沿用 App 既有深色面板與低亮度語意色。
- 已確認仍用低亮度完成色，高權限提示使用既有模式提示色，降低「另一套介面」的突兀感。
- 二分待轉與已完成數都會反映在特殊盤點「應有」預覽，並以文字提示目前待轉／完成數。

## 資料結構與非退版
- Schema 維持16；沒有新增第二份盤點資料。
- `events` 仍是中班／夜班／過境／一般提前載走／二分／現場的唯一正式數量來源。
- 0.051特殊盤點、0.050現場2×2資訊、0.049二分滑道顯示、0.047 00前已載走、0.046 05:00複製排除與Nt24全部保留。
- 0.048負數截斷仍不採用；一般提前載走大於來源時仍可顯示負應有數以暴露資料異常。
- 核心資產與 Service Worker 快取升版至0.052。

---
# 0.051 更新紀錄

## 新增：特殊盤點
- 新增「其他功能 → 特殊盤點」，定位為收線最後使用的高權限盤點入口。
- 特殊盤點不建立第二份中班／夜班／過境／現場數量；畫面直接讀取既有正式事件帳。
- 中班、夜班、過境、現場實盤採直接數字輸入；輸入先停留在記憶體草稿，只有按「確認此站」才正式寫回。
- 按確認時，以同一 operationId 的差額事件修正原本 `morning`／`night`／`transit`／`actual`，事件備註標記為「特殊盤點修正」。
- 正常中班、夜班產出、過境貨與現場盤點會立即讀到修正後結果；反向也是相同資料來源，因此一般流程的既有數字也會直接顯示在特殊盤點。

## 載具與現場核對
- 每站提供「預設載具／非預設載具」雙頁籤，可分別確認籠與板。
- 每個載具顯示中班／夜班／過境／現場四個輸入欄。
- 額外顯示只讀的「來源／載走／應有／差異」；來源會包含已轉完成的二次分理，並在有二分數量時註明。
- 一般提前載走仍由原 `loaded` 資料提供並照常扣除；特殊盤點不會重設提前載走。
- `00前已載走` 仍維持獨立資料結構，不會被特殊盤點改動。
- 二次分理 `online`／`secondary` 仍維持原邏輯，不會被特殊盤點改動。
- 現場應有仍保留真實計算結果，可為負數，不採用0.048的負數截斷。

## 確認與防呆
- 每站可按「確認此站」；即使現場數量為0，也會保留籠／板都已盤過的 actual 確認。
- 新增「已確認簽章」：簽章涵蓋中班、夜班、過境、一般提前載走、二分待完成、二分已完成、現場實盤。
- 特殊盤點確認後，只要正常流程再修改上述任何會影響核對的資料，該站的已確認狀態會自動失效，避免舊確認標記誤導。
- 確認資料只保存時間與簽章，不保存另一份數量。
- 新增「未確認」及「只顯示有差異」篩選，並保留 N／T／C／S／E 系列分類。

## 資料結構
- Schema 由15升至16。
- 班次只新增 `specialInventory: { confirmations: {} }` 作為確認狀態／簽章資料。
- 所有正式數量仍唯一保存於既有 `events`；沒有 `specialCounts`、沒有特殊盤點第二份數字。
- 舊Schema 15班次升級時自動建立空的特殊盤點確認結構，既有事件、00前已載走與現場確認資料不變。

## 非退版
- 0.050 現場盤點中／夜／過／總 2×2資訊卡完整保留。
- 0.049 二次分理「待處理固定顯示＋滑道臨時選站」完整保留。
- 0.047 00前已載走、最終籠／板總量整合完整保留。
- 0.046 05:00複製排除 NS6／TS1／TS3、NS15／Nt20滑道調整與Nt24完整保留。
- `makeReportText`、`makeTransitReportText`、`stationStats`、`computeStatsOverview` 與0.050相同。
- 核心資產與Service Worker快取升版至0.051。

---

# 0.050 更新紀錄

## 現場盤點：站所資訊加入夜班數量
- 以 0.049 為基底。
- 現場盤點站所卡右上來源資訊由「中／過／總」改為 2×2：「中／夜、過／總」。
- 新增夜班籠／板數顯示，直接使用既有 `computeAllStats` 的 night 數值，不新增或改動資料結構。
- 卡片資訊由原本三行壓成兩行，不增加卡片高度。
- 中班、夜班、過境維持低對比來源色；只有「總」繼續跟隨核對結果顯示綠／紅。
- 現場應有、提前載走扣除、負數異常、差異判定與輸入邏輯全部不變。
- Schema 維持 15；核心資產與 Service Worker 快取升版至 0.050。

---

# 0.049 更新紀錄

## 二次分理：待處理固定顯示＋滑道臨時選站
- 以 0.047 為基底；0.048 的「現場應有數最低為0」修正不採用，負數異常仍照原始資料顯示。
- 二次分理預設不再一次顯示第7～12滑道全部0數量站所。
- 上方新增「全部／7／8／9／10／11／12」滑道顯示切換；再次按同一按鈕即可收合。
- 滑道按鈕只負責臨時叫出可選站所，不做快速定位。
- 各滑道按鈕會低調顯示目前該滑道有待處理數量的站所數。
- 0數量站所在選站區改為精簡卡片，只顯示站名、預設載具與＋1，降低畫面雜訊。
- 站所一旦按＋1、待處理數量大於0，就移到「待處理」區並固定顯示，不受滑道收合影響。
- 待處理數量歸0或按「轉完成」後，站所不再固定顯示；若滑道仍展開，仍可在選站區再次加入。
- 「全部轉完成」仍只處理真正有待處理數量的站所；下方「已轉完成紀錄」完整保留。
- 非預設載具仍維持一次性模式，下一筆＋1後自動關閉。
- 本版只新增 UI 展開狀態，不改二次分理事件資料、Schema 15、00前已載走、統計或快速回報計算。
- 核心資產升版至 0.049，Service Worker 快取同步升版。

---

# 0.047 更新紀錄

## 00前已載走改為獨立資料來源
- 「提前載走」頁新增可收合的「00前已載走」專區。
- 常用站所預設為 NS6、TS3、HT1、YT1、ET3；不再使用已淘汰的 ET1／ET2 名稱。
- 00前已載走預設載具固定為籠；「非預設」只讓下一筆改用板，完成後自動恢復籠。
- 可透過 N／T／C／S／E 系列臨時加入其他站所；臨時站只屬於當班，不改永久預設名單。
- 臨時站數量歸零後才可移除，避免誤刪已有資料的站所。
- 支援「完成盤點」收合與「修改」重新開啟，降低後續工作誤觸風險。
- 00前已載走描述貨物於00:00前已離場；實際可在00:00後完成確認與輸入。

## 中班與統計資料流調整
- 中班數量頁完全移除舊「00前轉出總數」輸入與顯示。
- 00前已載走不列入中班站所數、不列入03:00／05:00快速回報，也不加入現場盤點的站所應有數。
- 統計核對的「中＋夜 籠數／板數」仍會把00前已載走加回最終總量，避免總籠數少算。
- 統計核對備註直接顯示本班00前已載走籠／板來源。
- 一般夜班期間「提前載走」仍沿用原 loaded 資料，不與00前已載走混存。

## 資料遷移
- Schema 由14升至15。
- 新增獨立 `preMidnightLoaded` 結構，保存分站籠／板、臨時站所與完成狀態。
- 舊版若已有「00前轉出總籠數」，升級時保留為「舊版未分站」籠數，不虛構站所明細；可在00前已載走專區手動清除後改錄分站資料。

## 非退版
- 0.046 的05:00複製排除 NS6／TS1／TS3、NS15／Nt20滑道調整與 Nt24 全部保留。
- 0.033快速回報全形固定欄寬格式不變。
- 核心資產升版至0.047，Service Worker快取同步升版。

---

# 0.046 更新紀錄

- 05:00快速回報畫面、統計與異常檢查不變；僅複製文字排除 NS6、TS1、TS3。
- NS15由第九滑道移至第八滑道。
- Nt20由第十滑道移至第九滑道。
- 新增Nt24於第十滑道，歸N系列並接在Nt23之後。
- Nt24預設載具為棧板；Schema維持14。
- 核心資產升版至0.046，Service Worker快取同步升版。

---

# 0.045 更新紀錄

- 二次分理刪除「全部 NS／Nt／TS／Tt 25站＋1」快速批次功能。
- 「全部待處理轉完成」縮短為「全部轉完成」，並與「非預設」合併成單排。
- 二次分理大型重複說明卡改為薄型「轉完成才計數量／操作說明」。
- 站所卡降低外框、背景與「無待處理」狀態的視覺強度；待處理時「轉完成」仍保持明確。
- 不改二次分理逐站增減、轉完成、統計及快速回報核心邏輯。
- 核心資產升版至 0.045，Service Worker 快取同步升版。

---

# 0.044 更新紀錄

## 中班數量盤點分類與數量0篩選

- 以已實機測試 OK 的 0.043 為唯一開發基底。
- 中班數量頁新增與統計核對相同概念的系列分類：全部／N系列／T系列／C系列／S系列／E系列。
- 新增「只顯示數量0」切換按鈕；判定方式為該站中班籠車＋棧板合計為0，避免只有非預設載具有數量時被誤判。
- 「只顯示數量0」旁顯示目前分類範圍內的0數量站所數。
- 啟用數量0篩選後，站所輸入成非0並完成儲存時，該站會自動從清單移除。

## 中班上方操作壓縮

- 「複製中班各站所籠／板數」縮短為「複製站所」。
- 「下一筆改用非預設載具」縮短為「非預設」；啟用時顯示「非預設：開」。
- 複製與非預設改為左右同排。
- 系列分類與「只顯示數量0」另成左右同排，維持手機畫面簡潔。

## 非退版

- 0.043 夜班第7～12滑道目前位置微亮提示完整保留。
- 0.042 夜班雙按鈕、sticky滑道定位與scroll-margin完整保留。
- 0.033快速回報複製格式維持不變。
- 資料結構仍為Schema 14；只新增UI偏好欄位，不更動班次／事件資料格式。
- 站所、滑道排序、預設載具與既有計數邏輯均未更動。
- 核心資產改用0.044實體檔名，Service Worker快取名稱同步升版。

---

# 0.043 更新紀錄

## 夜班滑道目前位置提示

- 以 0.042 為唯一開發基底。
- 第 7／8／9／10／11／12 滑道快速定位列新增「目前區塊」微亮提示。
- 畫面捲動進入某滑道時，對應數字按鈕會自動切換為低亮度藍灰選取狀態。
- 按定位按鈕時會先立即微亮，平滑捲動期間再依實際所在區塊自動更新。
- 使用既有語意色盤的低亮度主色，不使用高飽和或高亮度顏色，維持整體深色介面風格。
- 一般夜班＋1模式與修正數量模式都支援目前滑道提示。

## 非退版

- 0.042 的雙按鈕、sticky 滑道定位與 scroll-margin 全部保留。
- 0.033 快速回報複製格式維持不變。
- 資料結構仍為 Schema 14。
- 站所、滑道排序、預設載具與既有計數邏輯均未更動。
- 核心資產改用 0.043 實體檔名，Service Worker 快取名稱同步升版。

---

# 0.042 更新紀錄

## 夜班頁操作精簡

- 以 0.041 為唯一開發基底。
- 「修正前面數量」縮短為「修正數量」。
- 「下一筆改用非預設載具」縮短為「非預設」；啟用時顯示「非預設：開」。
- 兩個功能改為同一排左右雙按鈕，降低上方操作區高度。
- 夜班說明縮短為「點站所＋1」與「特殊載具先按『非預設』」。

## 滑道快速定位

- 新增 7／8／9／10／11／12 六個滑道定位按鈕。
- 按鈕只負責定位，不會隱藏或篩選任何站所。
- 按下數字會捲動到對應滑道區塊。
- 滑道定位列使用 sticky，往下滑時持續固定在頂部標題列下方。
- 每個滑道區塊加入 scroll-margin，避免定位後標題被固定列遮住。
- 修正模式同樣保留滑道快速定位。

## 非退版

- 0.033 快速回報複製格式維持不變。
- 資料結構仍為 Schema 14。
- 站所、滑道排序、預設載具與既有計數邏輯均未更動。
- 核心資產改用 0.042 實體檔名，Service Worker 快取名稱同步升版。
